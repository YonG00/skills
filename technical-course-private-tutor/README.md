# 技术课程私人导师（Technical Course Private Tutor）

一个用于**逐节精读技术课程材料**的 Agent Skill。

它把 lecture notes、讲义 PDF、视频转录稿、GitHub chapter book、论文配套课程材料，重建成**一条可以一节一节走完的连续知识路径**——而不是一份读完就忘的摘要。

> 本仓库对应 `SKILL.md`（执行形态）与 `README.md`（可阅读的形态）。二者来自同一套知识体系：**Skill 是可执行的方法，Blog 是可阅读的方法。**

---

## 它解决什么问题

把一份技术讲义丢给通用模型，最常见的失败不是"讲错"，而是**讲得不像在教人**：

| 失败模式 | 表现 |
|---|---|
| 用术语代替理解 | 「模型表现可以分成 capability 和 behavior 两层」——但学生还没看出这两者有什么区别 |
| 抢跑 | 第 3 节就开始解释第 22 节的 DPO loss，因为"顺便说一下会更完整" |
| 一节塞多个任务 | 学生读完只记得"讲了很多东西"，说不出到底多懂了哪一件 |
| 板书腔 | 普通因果关系被拆成 `long SFT data ↓ longer responses ↓ higher preference` |
| 目录推动 | 按"定义 → 数学形式 → 优点 → 缺点 → Benchmark → 延伸"展开，而不是按学生此刻真正产生的问题展开 |
| 为了完整而继续 | 核心已经讲清楚，却继续加例子、加类比、加 benchmark |
| 无来源的发明者 | 凭印象写"某某在 20XX 年提出"，年份或作者是错的，形成更难纠正的记忆 |
| 越更新越臃肿 | Skill 变成堆满一次性案例和固定答案的仓库，而不是越来越会判断 |

本 skill 的每一条规则都对应上面某一种可重复出现的失败模式——这是它的准入标准，不是装饰。

---

## 核心主张

### 1. 三条元原则

```
课程材料决定主线   →  认知依赖决定顺序   →  当前小节决定边界
```

> **路线图负责让学生知道未来会学什么；当前正文负责让学生只学现在该学的东西。**

### 2. 一节 = 一个认知问题，而不是一个主题

写正文前必须先能说出：**「这一节其实就是：________。」**
如果说不清，说明这一节仍然包含多个教学任务，应继续拆分。

### 3. 先看见区别，再给概念

`具体冲突 / 两个例子 → 学生看出差别 → 给这个差别命名 → 解释为什么 → 回到课程材料`

概念名称应该帮助学生**压缩已经形成的理解**，而不是代替理解本身。

### 4. 新技术第一次出现时，给一个"历史锚点"

用 1–3 句话回答：**什么时候出现的？谁提出/发布？当时为什么需要它？**

目标是给概念加一个长期记忆挂钩：`概念 + 时间 + 人/机构 + 当时的问题`。学完整门课后，学生脑中应自然浮现一条粗略技术线，而不是一串互不相干的方法名。

但历史锚点必须**服务当前认知问题**——如果它开始喧宾夺主，就应该停止。

### 5. 每节以固定的四件套结束，然后停下

一句话总结（压回最朴素的说法）+ 进度 + **一个**理解检查（不给答案）+ 克制的下一节预告（1–2 句，只提问题）。

---

## 目录结构

```
technical-course-private-tutor/
├── SKILL.md                          # 可执行规范（主文件）
├── README.md                         # 本文件：为什么这样设计
├── CHANGELOG.md                      # 版本差异 v2 → v3.2
├── LICENSE
└── references/
    ├── base-teaching-v2.md           # 基础教师能力（v2 原文要点，防止规则静默丢失）
    ├── skill-maintenance.md          # Skill 自我更新方法论
    ├── history-anchor-examples.md    # 历史锚点示例（示例不作为模板）
    └── reference-registry.md         # 外部知识入口索引
```

**规则资产 / 知识资产分离**：`SKILL.md` 只保存判断方法；原始来源保存在 `references/`，作为"外部知识入口"——需要准确细节时沿着链接回到原文，而不是依赖压缩后的二手记忆。

---

## 安装 / 使用

作为一个 Agent Skill 使用：把 `technical-course-private-tutor/` 整个目录放进你的 skills 目录即可（`SKILL.md` 带有 `name` / `description` frontmatter）。

作为提示词使用：直接读取并遵循 `SKILL.md`。

典型触发方式：

```
带我逐节学这份讲义：<链接 / 文件>
```

然后按 `继续 / 下一节 / 往下` 推进。

---

## 版本沿革

本 skill 是对博客园作者 yong_2333 的「技术课程私人导师」系列的**合并重述**（v2 基础 + v3 / v3.1 / v3.2 三次增量），并补齐了版本裁决与来源索引。

| 版本 | 主要新增 |
|---|---|
| **v2** | 基础教师能力：课程主线、认知依赖切分、单节七构件、跨节抢跑禁令、数学/代码规则、每节三件套 |
| **v3** | 单节信息边界与删除测试；下一节"钩子"的尺度；新概念首次出现的**来源与时间**规则；Reference Registry；执行前快速检查 |
| **v3.1** | 单节目标从"一个主题"收紧为"**一个认知问题**"；先看见区别再给概念；对照教学；反板书腔；问题推动而非目录推动；教学压缩检查；反馈触发的即时校正 |
| **v3.2** | **历史锚点**规则：1–3 句交代"什么时候、谁、为什么出现"；区分提出/发布/开源/推广；不确定必须查一手来源 |

上游各版本均采用"增量继承"写法（新版只写差异，旧版规则继续有效，冲突以新版为准）。本仓库把这条链**摊平为一份自洽的规范**，同时用 `references/base-teaching-v2.md` 保留 v2 原始要点，避免合并过程中旧规则静默消失。

详见 [`CHANGELOG.md`](CHANGELOG.md)。

---

## 设计来源

本 skill 的方法论与规则全部来自上游博客，原始链接保存在 [`references/reference-registry.md`](references/reference-registry.md)：

- 技术课程私人导师 v2 — https://www.cnblogs.com/yong2333/articles/22986095
- 技术课程私人导师 v3 — https://www.cnblogs.com/yong2333/articles/22998999
- 技术课程私人导师 v3.1 — https://www.cnblogs.com/yong2333/articles/23002078
- 技术课程私人导师 v3.2 — https://www.cnblogs.com/yong2333/articles/23016300
- Skill 更新、博客化与外部知识资产原则 — https://www.cnblogs.com/yong2333/articles/22998468

本文档（README）即该系列所主张的"**博客形态**"输出；`SKILL.md` 是其"**执行形态**"。

---

## 更新本 skill

按 [`references/skill-maintenance.md`](references/skill-maintenance.md) 的工作流：

```
收集反馈 → 区分局部/任务/通用 → 跨主题通用性测试 → 建立旧规则清单
→ 优先扩展或合并 → 检查旧规则是否丢失 → 冲突交给用户决定
→ 更新 SKILL.md → 更新 CHANGELOG.md → 新来源写入 reference-registry.md
```

三条硬约束：

1. **每条规则必须对应一个失败模式**——不为"看起来更完整"添加规则。
2. **旧规则默认保留**——重写不等于重新发明。
3. **新旧规则冲突不擅自决定**——列出冲突点，交给用户裁决。

---

## 还有哪些没有解决

- **历史锚点的事实核实依赖联网能力**。没有联网时只能执行 SKILL.md §7.5 的保守策略（明确说"不确定"），不能凭记忆补年份和作者。
- **15–30 节的主干切分是经验值**，对超长课程（整学期）需要分段规划，本 skill 尚未定义分段规则。
- **理解检查的难度校准**没有客观标准，目前依赖 §5.9 的定性要求。
- 本仓库尚未提供可运行的校验脚本；`§11` 的检查清单目前是**模型自检**，不是自动化测试。
